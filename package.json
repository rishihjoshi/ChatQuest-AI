{
  "name": "chatquest-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Side-by-side multi-model AI chat PWA. One prompt, N models, N streaming panes.",
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-icons.js && node scripts/generate-version.js",
    "dev": "vercel dev",
    "verify": "node scripts/verify-models.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
